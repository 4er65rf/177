limit = 1010

par = []
impar = list ()

for num in range (1,limite+1):
    if num % 2 == 0: 
        par.append(num)
    else:
        impar.append(num)

impresion = max(len(par),len(impar))
print(impresion)

for item in range (impresion):
    try:
        print(par[])
        
